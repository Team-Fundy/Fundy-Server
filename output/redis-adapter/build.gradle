project(":output:redis-adapter") {
    bootJar{enabled = false}
    jar {enabled = true}

    dependencies {
        implementation project(":application:user-service")
        api 'org.springframework.boot:spring-boot-starter-data-redis:3.1.2'
    }
}